"use server";

// import { createUser } from "@/lib/dynamodb/race";

// export async function createUserAction(formData: FormData) {
//     await createUser({
//         id: crypto.randomUUID(),
//         name: formData.get("name"),
//     });
// }